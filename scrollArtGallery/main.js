// I tried the "wheel" method which contained in the reference video
// sadly it didn't work well, it's not smooth at all.
// then I turned into "scroll"

// let imgs = document.querySelector(`.imgs`);

// function next() {
//   let img = documents.querySelectorAll(`img`);
//   imgs.appendChild(img[0]);
// }

// function prev() {
//   let img = document.querySelector(`img`);
//   imgs.appendChild(img[imgs.length - 1]);
// }

// window.addEventListener("wheel", function (event) {
//   if (event.deltaY > 0) {
//     next();
//   } else {
//     prev();
//   }
// });
